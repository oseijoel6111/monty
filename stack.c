#include "monty.h"
/**
 * fStack - prints the top
 * @head: stack head
 * @counters: line_number
 * Return: no return
*/
void fStack(stack_t **head, unsigned int counters)
{
	(void)head;
	(void)counters;
	bus.lifi = 0;
}
